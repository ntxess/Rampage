#pragma once

#include "../common/modifiers/Modification.hpp"
#include <vector>

struct ModPool
{
	//std::vector<std::unique_ptr<Modification>> pool;

	//ModPool() {}

	//void add(std::unique_ptr<Modification>& mods)
	//{
	//	pool.push_back(std::move(mods));
	//}
};